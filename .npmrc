node-linker=hoisted

# ── Supply-chain security ─────────────────────────────────
# Legitimate packages that need lifecycle scripts (e.g. native compilers)
# are explicitly allowed in package.json (pnpm.onlyBuiltDependencies).
